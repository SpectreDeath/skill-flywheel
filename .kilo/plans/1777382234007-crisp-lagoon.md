# Plan: Extend reindex_skills.py with multi-logic surface support

## Objective
Add multi-logic surface support to `src/flywheel/core/reindex_skills.py` per the provided reference implementation, adhering to the **additive-only** constraint (no changes to existing fields or logic).

## Current State Analysis
- Existing file (76 lines) has:
  - Imports (lines 1-6), constants (lines 8-10)
  - `get_skill_metadata()` (lines 13-51) returning 7 fixed fields: `name`, `domain`, `version`, `purpose`, `description`, `path`, `last_modified`
  - `reindex()` (lines 54-72) with basic summary output
- No existing extractor functions for fenced blocks, Prolog tags, or Hy tags
- No `surfaces`, `logic_tags`, or `heuristic_tags` fields in metadata or summary

## Implementation Steps (Additive Only)

### 1. Add New Extractor Functions
Insert after line 10 (after constants, before `get_skill_metadata`):
- `extract_fenced_block(content, lang)`: Regex to extract first fenced code block matching `lang` tag using pattern `rf"```{lang}\s*\n(.*?)```"` with `re.DOTALL`
- `extract_prolog_tags(prolog_source)`: Extract deduplicated predicate names from Prolog clause heads (regex `r"^([a-z][a-zA-Z0-9_]*)\s*[:(]"` with `re.MULTILINE`), exclude builtins `{"not", "is", "true", "fail", "assert", "retract"}`
- `extract_hy_tags(hy_source)`: Extract deduplicated function names from Hy `defn` forms (regex `r"\(defn\s+([a-zA-Z][a-zA-Z0-9_\-?!]*)"`)

### 2. Extend `get_skill_metadata()` (Additive)
- Add `body = parts[2]` after line 26 (reuse existing body content for purpose/description extraction)
- Call extractors on `body` to get `prolog_source`, `hy_source`, `python_source`
- Build `surfaces` list: append `"python"` if `python_source` exists, `"prolog"` if `prolog_source` exists, `"hy"` if `hy_source` exists
- Fall back to `fm.get("surfaces", ["python"])` if `surfaces` is empty
- Compute `logic_tags = extract_prolog_tags(prolog_source)` and `heuristic_tags = extract_hy_tags(hy_source)`
- Append 3 new keys to the returned dict (keep all 7 existing keys intact):
  - `"surfaces": surfaces`
  - `"logic_tags": logic_tags`
  - `"heuristic_tags": heuristic_tags`

### 3. Extend `reindex()` Summary (Additive)
Add after line 72 (current print statement):
- Calculate multi-surface count: `multi = [s for s in registry if len(s.get("surfaces", [])) > 1]`
- Print:
  - `f"  Multi-surface skills: {len(multi)}"`
  - `f"  Single-surface skills: {len(registry) - len(multi)}"`

## Validation
1. Run `python src/flywheel/core/reindex_skills.py` to verify no runtime errors
2. Check `skill_registry.json` confirms new fields exist and existing fields are unchanged
3. Verify summary output shows multi/single-surface counts
