"""
Auto-generated skill for pandas

This skill wraps the public API of the pandas library.
Generated on: 2026-03-20T19:12:02.937709
Library version: 2.3.3

Wrapped components: 102

Usage:
    Invoke without action to list available wrappers:
        await invoke({"action": None})
    
    Invoke a specific wrapper:
        await invoke({"action": "function_name", "args": [arg1, arg2], "kwargs": {"key": "value"}})
"""
import inspect
import logging
from typing import Any, Dict, List

import time
import datetime

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

WRAPPED_NAMES = ["ArrowDtype", "BooleanDtype", "Categorical", "CategoricalDtype", "CategoricalIndex", "DataFrame", "DateOffset", "DatetimeIndex", "DatetimeTZDtype", "ExcelFile", "ExcelWriter", "Flags", "Float32Dtype", "Float64Dtype", "Grouper", "HDFStore", "Index", "Int16Dtype", "Int32Dtype", "Int64Dtype", "Int8Dtype", "Interval", "IntervalDtype", "IntervalIndex", "MultiIndex", "NamedAgg", "Period", "PeriodDtype", "PeriodIndex", "RangeIndex", "Series", "SparseDtype", "StringDtype", "Timedelta", "TimedeltaIndex", "Timestamp", "UInt16Dtype", "UInt32Dtype", "UInt64Dtype", "UInt8Dtype", "array", "bdate_range", "concat", "crosstab", "cut", "date_range", "describe_option", "eval", "factorize", "from_dummies", "get_dummies", "get_option", "infer_freq", "interval_range", "isna", "isnull", "json_normalize", "lreshape", "melt", "merge", "merge_asof", "merge_ordered", "notna", "notnull", "option_context", "period_range", "pivot", "pivot_table", "qcut", "read_clipboard", "read_csv", "read_excel", "read_feather", "read_fwf", "read_gbq", "read_hdf", "read_html", "read_json", "read_orc", "read_parquet", "read_pickle", "read_sas", "read_spss", "read_sql", "read_sql_query", "read_sql_table", "read_stata", "read_table", "read_xml", "reset_option", "set_eng_float_format", "set_option", "show_versions", "test", "timedelta_range", "to_datetime", "to_numeric", "to_pickle", "to_timedelta", "unique", "value_counts", "wide_to_long"]

async def ArrowDtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.ArrowDtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "ArrowDtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BooleanDtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.BooleanDtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "BooleanDtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Categorical_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Categorical
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Categorical")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def CategoricalDtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.CategoricalDtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "CategoricalDtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def CategoricalIndex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.CategoricalIndex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "CategoricalIndex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def DataFrame_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.DataFrame
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "DataFrame")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def DateOffset_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.DateOffset
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "DateOffset")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def DatetimeIndex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.DatetimeIndex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "DatetimeIndex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def DatetimeTZDtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.DatetimeTZDtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "DatetimeTZDtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ExcelFile_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.ExcelFile
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "ExcelFile")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ExcelWriter_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.ExcelWriter
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "ExcelWriter")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Flags_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Flags
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Flags")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Float32Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Float32Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Float32Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Float64Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Float64Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Float64Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Grouper_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Grouper
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Grouper")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def HDFStore_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.HDFStore
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "HDFStore")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Index_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Index
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Index")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Int16Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Int16Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Int16Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Int32Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Int32Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Int32Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Int64Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Int64Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Int64Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Int8Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Int8Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Int8Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Interval_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Interval
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Interval")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def IntervalDtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.IntervalDtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "IntervalDtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def IntervalIndex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.IntervalIndex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "IntervalIndex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def MultiIndex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.MultiIndex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "MultiIndex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def NamedAgg_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.NamedAgg
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "NamedAgg")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Period_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Period
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Period")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def PeriodDtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.PeriodDtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "PeriodDtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def PeriodIndex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.PeriodIndex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "PeriodIndex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def RangeIndex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.RangeIndex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "RangeIndex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Series_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Series
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Series")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def SparseDtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.SparseDtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "SparseDtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def StringDtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.StringDtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "StringDtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Timedelta_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Timedelta
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Timedelta")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TimedeltaIndex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.TimedeltaIndex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "TimedeltaIndex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Timestamp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.Timestamp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "Timestamp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def UInt16Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.UInt16Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "UInt16Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def UInt32Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.UInt32Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "UInt32Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def UInt64Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.UInt64Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "UInt64Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def UInt8Dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.UInt8Dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "UInt8Dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def array_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.array
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "array")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bdate_range_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.bdate_range
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "bdate_range")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def concat_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.concat
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "concat")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def crosstab_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.crosstab
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "crosstab")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cut_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.cut
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "cut")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def date_range_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.date_range
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "date_range")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def describe_option_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.describe_option
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "describe_option")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def eval_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.eval
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "eval")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def factorize_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.factorize
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "factorize")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def from_dummies_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.from_dummies
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "from_dummies")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def get_dummies_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.get_dummies
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "get_dummies")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def get_option_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.get_option
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "get_option")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def infer_freq_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.infer_freq
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "infer_freq")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def interval_range_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.interval_range
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "interval_range")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isna_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.isna
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "isna")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isnull_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.isnull
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "isnull")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def json_normalize_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.json_normalize
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "json_normalize")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def lreshape_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.lreshape
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "lreshape")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def melt_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.melt
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "melt")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def merge_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.merge
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "merge")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def merge_asof_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.merge_asof
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "merge_asof")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def merge_ordered_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.merge_ordered
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "merge_ordered")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def notna_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.notna
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "notna")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def notnull_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.notnull
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "notnull")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def option_context_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.option_context
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "option_context")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def period_range_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.period_range
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "period_range")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def pivot_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.pivot
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "pivot")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def pivot_table_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.pivot_table
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "pivot_table")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def qcut_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.qcut
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "qcut")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_clipboard_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_clipboard
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_clipboard")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_csv_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_csv
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_csv")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_excel_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_excel
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_excel")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_feather_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_feather
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_feather")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_fwf_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_fwf
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_fwf")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_gbq_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_gbq
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_gbq")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_hdf_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_hdf
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_hdf")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_html_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_html
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_html")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_json_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_json
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_json")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_orc_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_orc
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_orc")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_parquet_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_parquet
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_parquet")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_pickle_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_pickle
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_pickle")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_sas_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_sas
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_sas")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_spss_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_spss
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_spss")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_sql_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_sql
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_sql")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_sql_query_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_sql_query
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_sql_query")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_sql_table_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_sql_table
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_sql_table")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_stata_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_stata
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_stata")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_table_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_table
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_table")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def read_xml_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.read_xml
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "read_xml")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def reset_option_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.reset_option
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "reset_option")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def set_eng_float_format_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.set_eng_float_format
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "set_eng_float_format")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def set_option_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.set_option
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "set_option")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def show_versions_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.show_versions
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "show_versions")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def test_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.test
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "test")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def timedelta_range_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.timedelta_range
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "timedelta_range")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def to_datetime_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.to_datetime
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "to_datetime")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def to_numeric_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.to_numeric
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "to_numeric")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def to_pickle_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.to_pickle
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "to_pickle")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def to_timedelta_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.to_timedelta
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "to_timedelta")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unique_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.unique
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "unique")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def value_counts_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.value_counts
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "value_counts")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def wide_to_long_wrapper(args: list, kwargs: dict):
    """
    Wrapper for pandas.wide_to_long
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import pandas
        member = getattr(pandas, "wide_to_long")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def invoke(payload: Dict[str, Any]) -> Dict[str, Any]:
    """
    Main entry point for skill invocation.
    
    Expected payload:
        - action: str (optional): Name of wrapper to call. If omitted, returns list of available wrappers
        - args: list (optional): Positional arguments for the wrapper
        - kwargs: dict (optional): Keyword arguments for the wrapper
    
    Returns:
        dict with 'result' and 'metadata' keys
    """
    start_time = time.time()
    timestamp = datetime.datetime.now().isoformat()
    
    action = payload.get("action")
    args = payload.get("args", [])
    kwargs = payload.get("kwargs", {})
    
    if action is None:
        return {
            "result": {
                "available_wrappers": list(WRAPPED_NAMES),
                "message": "Available wrappers. Use action parameter to call a specific wrapper."
            },
            "metadata": {
                "timestamp": timestamp,
                "library": "pandas",
                "function_count": len(WRAPPED_NAMES)
            }
        }
    
    if action not in WRAPPED_NAMES:
        return {
            "result": {"error": "Unknown action: " + action},
            "metadata": {
                "timestamp": timestamp,
                "error": "Action not found in wrapped names"
            }
        }
    
    wrappers = {
        "ArrowDtype": ArrowDtype_wrapper,
        "BooleanDtype": BooleanDtype_wrapper,
        "Categorical": Categorical_wrapper,
        "CategoricalDtype": CategoricalDtype_wrapper,
        "CategoricalIndex": CategoricalIndex_wrapper,
        "DataFrame": DataFrame_wrapper,
        "DateOffset": DateOffset_wrapper,
        "DatetimeIndex": DatetimeIndex_wrapper,
        "DatetimeTZDtype": DatetimeTZDtype_wrapper,
        "ExcelFile": ExcelFile_wrapper,
        "ExcelWriter": ExcelWriter_wrapper,
        "Flags": Flags_wrapper,
        "Float32Dtype": Float32Dtype_wrapper,
        "Float64Dtype": Float64Dtype_wrapper,
        "Grouper": Grouper_wrapper,
        "HDFStore": HDFStore_wrapper,
        "Index": Index_wrapper,
        "Int16Dtype": Int16Dtype_wrapper,
        "Int32Dtype": Int32Dtype_wrapper,
        "Int64Dtype": Int64Dtype_wrapper,
        "Int8Dtype": Int8Dtype_wrapper,
        "Interval": Interval_wrapper,
        "IntervalDtype": IntervalDtype_wrapper,
        "IntervalIndex": IntervalIndex_wrapper,
        "MultiIndex": MultiIndex_wrapper,
        "NamedAgg": NamedAgg_wrapper,
        "Period": Period_wrapper,
        "PeriodDtype": PeriodDtype_wrapper,
        "PeriodIndex": PeriodIndex_wrapper,
        "RangeIndex": RangeIndex_wrapper,
        "Series": Series_wrapper,
        "SparseDtype": SparseDtype_wrapper,
        "StringDtype": StringDtype_wrapper,
        "Timedelta": Timedelta_wrapper,
        "TimedeltaIndex": TimedeltaIndex_wrapper,
        "Timestamp": Timestamp_wrapper,
        "UInt16Dtype": UInt16Dtype_wrapper,
        "UInt32Dtype": UInt32Dtype_wrapper,
        "UInt64Dtype": UInt64Dtype_wrapper,
        "UInt8Dtype": UInt8Dtype_wrapper,
        "array": array_wrapper,
        "bdate_range": bdate_range_wrapper,
        "concat": concat_wrapper,
        "crosstab": crosstab_wrapper,
        "cut": cut_wrapper,
        "date_range": date_range_wrapper,
        "describe_option": describe_option_wrapper,
        "eval": eval_wrapper,
        "factorize": factorize_wrapper,
        "from_dummies": from_dummies_wrapper,
        "get_dummies": get_dummies_wrapper,
        "get_option": get_option_wrapper,
        "infer_freq": infer_freq_wrapper,
        "interval_range": interval_range_wrapper,
        "isna": isna_wrapper,
        "isnull": isnull_wrapper,
        "json_normalize": json_normalize_wrapper,
        "lreshape": lreshape_wrapper,
        "melt": melt_wrapper,
        "merge": merge_wrapper,
        "merge_asof": merge_asof_wrapper,
        "merge_ordered": merge_ordered_wrapper,
        "notna": notna_wrapper,
        "notnull": notnull_wrapper,
        "option_context": option_context_wrapper,
        "period_range": period_range_wrapper,
        "pivot": pivot_wrapper,
        "pivot_table": pivot_table_wrapper,
        "qcut": qcut_wrapper,
        "read_clipboard": read_clipboard_wrapper,
        "read_csv": read_csv_wrapper,
        "read_excel": read_excel_wrapper,
        "read_feather": read_feather_wrapper,
        "read_fwf": read_fwf_wrapper,
        "read_gbq": read_gbq_wrapper,
        "read_hdf": read_hdf_wrapper,
        "read_html": read_html_wrapper,
        "read_json": read_json_wrapper,
        "read_orc": read_orc_wrapper,
        "read_parquet": read_parquet_wrapper,
        "read_pickle": read_pickle_wrapper,
        "read_sas": read_sas_wrapper,
        "read_spss": read_spss_wrapper,
        "read_sql": read_sql_wrapper,
        "read_sql_query": read_sql_query_wrapper,
        "read_sql_table": read_sql_table_wrapper,
        "read_stata": read_stata_wrapper,
        "read_table": read_table_wrapper,
        "read_xml": read_xml_wrapper,
        "reset_option": reset_option_wrapper,
        "set_eng_float_format": set_eng_float_format_wrapper,
        "set_option": set_option_wrapper,
        "show_versions": show_versions_wrapper,
        "test": test_wrapper,
        "timedelta_range": timedelta_range_wrapper,
        "to_datetime": to_datetime_wrapper,
        "to_numeric": to_numeric_wrapper,
        "to_pickle": to_pickle_wrapper,
        "to_timedelta": to_timedelta_wrapper,
        "unique": unique_wrapper,
        "value_counts": value_counts_wrapper,
        "wide_to_long": wide_to_long_wrapper,
    }
    
    try:
        result = await wrappers[action](args, kwargs)
        elapsed = time.time() - start_time
        
        return {
            "result": result,
            "metadata": {
                "timestamp": timestamp,
                "library": "pandas",
                "library_version": "2.3.3" if "2.3.3" != "None" else None,
                "action": action,
                "elapsed_seconds": elapsed
            }
        }
    except Exception as e:
        logger.error("Error invoking {}: {}".format(action, str(e)))
        return {
            "result": {"error": str(e)},
            "metadata": {
                "timestamp": timestamp,
                "library": "pandas",
                "error": str(e)
            }
        }


def register_skill():
    """Return skill metadata for registration"""
    return {
        "name": "pandas-wrapper",
        "description": "Auto-generated skill wrapping pandas public API",
        "domain": "data_engineering",
        "version": "1.0.0",
    }
