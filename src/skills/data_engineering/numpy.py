"""
Auto-generated skill for numpy

This skill wraps the public API of the numpy library.
Generated on: 2026-03-20T19:12:37.253508
Library version: 2.4.2

Wrapped components: 460

Usage:
    Invoke without action to list available wrappers:
        await invoke({"action": None})
    
    Invoke a specific wrapper:
        await invoke({"action": "function_name", "args": [arg1, arg2], "kwargs": {"key": "value"}})
"""
import inspect
import logging
from typing import Any, Dict, List

import time
import datetime

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

WRAPPED_NAMES = ["abs", "absolute", "acos", "acosh", "add", "all", "allclose", "amax", "amin", "angle", "any", "append", "apply_along_axis", "apply_over_axes", "arange", "arccos", "arccosh", "arcsin", "arcsinh", "arctan", "arctan2", "arctanh", "argmax", "argmin", "argpartition", "argsort", "argwhere", "around", "array", "array2string", "array_equal", "array_equiv", "array_repr", "array_split", "array_str", "asanyarray", "asarray", "asarray_chkfinite", "ascontiguousarray", "asfortranarray", "asin", "asinh", "asmatrix", "astype", "atan", "atan2", "atanh", "atleast_1d", "atleast_2d", "atleast_3d", "average", "bartlett", "base_repr", "binary_repr", "bincount", "bitwise_and", "bitwise_count", "bitwise_invert", "bitwise_left_shift", "bitwise_not", "bitwise_or", "bitwise_right_shift", "bitwise_xor", "blackman", "block", "bmat", "bool", "bool_", "broadcast", "broadcast_arrays", "broadcast_shapes", "broadcast_to", "busday_count", "busday_offset", "busdaycalendar", "byte", "bytes_", "can_cast", "cbrt", "cdouble", "ceil", "character", "choose", "clip", "clongdouble", "column_stack", "common_type", "complex128", "complex64", "complexfloating", "compress", "concat", "concatenate", "conj", "conjugate", "convolve", "copy", "copysign", "copyto", "corrcoef", "correlate", "cos", "cosh", "count_nonzero", "cov", "cross", "csingle", "cumprod", "cumsum", "cumulative_prod", "cumulative_sum", "datetime64", "datetime_as_string", "datetime_data", "deg2rad", "degrees", "delete", "diag", "diag_indices", "diag_indices_from", "diagflat", "diagonal", "diff", "digitize", "divide", "divmod", "dot", "double", "dsplit", "dstack", "dtype", "ediff1d", "einsum", "einsum_path", "empty", "empty_like", "equal", "errstate", "exp", "exp2", "expand_dims", "expm1", "extract", "eye", "fabs", "fill_diagonal", "finfo", "fix", "flatiter", "flatnonzero", "flexible", "flip", "fliplr", "flipud", "float16", "float32", "float64", "float_power", "floating", "floor", "floor_divide", "fmax", "fmin", "fmod", "format_float_positional", "format_float_scientific", "frexp", "from_dlpack", "frombuffer", "fromfile", "fromfunction", "fromiter", "frompyfunc", "fromregex", "fromstring", "full", "full_like", "gcd", "generic", "genfromtxt", "geomspace", "get_include", "get_printoptions", "getbufsize", "geterr", "geterrcall", "gradient", "greater", "greater_equal", "half", "hamming", "hanning", "heaviside", "histogram", "histogram2d", "histogram_bin_edges", "histogramdd", "hsplit", "hstack", "hypot", "i0", "identity", "iinfo", "imag", "indices", "inexact", "info", "inner", "insert", "int16", "int32", "int64", "int8", "int_", "intc", "integer", "interp", "intersect1d", "intp", "invert", "is_busday", "isclose", "iscomplex", "iscomplexobj", "isdtype", "isfinite", "isfortran", "isin", "isinf", "isnan", "isnat", "isneginf", "isposinf", "isreal", "isrealobj", "isscalar", "issubdtype", "iterable", "ix_", "kaiser", "kron", "lcm", "ldexp", "left_shift", "less", "less_equal", "lexsort", "linspace", "load", "loadtxt", "log", "log10", "log1p", "log2", "logaddexp", "logaddexp2", "logical_and", "logical_not", "logical_or", "logical_xor", "logspace", "long", "longdouble", "longlong", "mask_indices", "matmul", "matrix", "matrix_transpose", "matvec", "max", "maximum", "may_share_memory", "mean", "median", "memmap", "meshgrid", "min", "min_scalar_type", "minimum", "mintypecode", "mod", "modf", "moveaxis", "multiply", "nan_to_num", "nanargmax", "nanargmin", "nancumprod", "nancumsum", "nanmax", "nanmean", "nanmedian", "nanmin", "nanpercentile", "nanprod", "nanquantile", "nanstd", "nansum", "nanvar", "ndarray", "ndenumerate", "ndim", "ndindex", "nditer", "negative", "nested_iters", "nextafter", "nonzero", "not_equal", "number", "object_", "ones", "ones_like", "outer", "packbits", "pad", "partition", "percentile", "permute_dims", "piecewise", "place", "poly", "poly1d", "polyadd", "polyder", "polydiv", "polyfit", "polyint", "polymul", "polysub", "polyval", "positive", "pow", "power", "printoptions", "prod", "promote_types", "ptp", "put", "put_along_axis", "putmask", "quantile", "rad2deg", "radians", "ravel", "ravel_multi_index", "real", "real_if_close", "recarray", "reciprocal", "record", "remainder", "repeat", "require", "reshape", "resize", "result_type", "right_shift", "rint", "roll", "rollaxis", "roots", "rot90", "round", "row_stack", "save", "savetxt", "savez", "savez_compressed", "searchsorted", "select", "set_printoptions", "setbufsize", "setdiff1d", "seterr", "seterrcall", "setxor1d", "shape", "shares_memory", "short", "show_config", "show_runtime", "sign", "signbit", "signedinteger", "sin", "sinc", "single", "sinh", "size", "sort", "sort_complex", "spacing", "split", "sqrt", "square", "squeeze", "stack", "std", "str_", "subtract", "sum", "swapaxes", "take", "take_along_axis", "tan", "tanh", "tensordot", "test", "tile", "timedelta64", "trace", "transpose", "trapezoid", "tri", "tril", "tril_indices", "tril_indices_from", "trim_zeros", "triu", "triu_indices", "triu_indices_from", "true_divide", "trunc", "typename", "ubyte", "ufunc", "uint", "uint16", "uint32", "uint64", "uint8", "uintc", "uintp", "ulong", "ulonglong", "union1d", "unique", "unique_all", "unique_counts", "unique_inverse", "unique_values", "unpackbits", "unravel_index", "unsignedinteger", "unstack", "unwrap", "ushort", "vander", "var", "vdot", "vecdot", "vecmat", "vectorize", "void", "vsplit", "vstack", "where", "zeros", "zeros_like"]

async def abs_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.abs
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "abs")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def absolute_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.absolute
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "absolute")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def acos_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.acos
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "acos")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def acosh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.acosh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "acosh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def add_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.add
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "add")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def all_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.all
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "all")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def allclose_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.allclose
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "allclose")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def amax_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.amax
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "amax")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def amin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.amin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "amin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def angle_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.angle
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "angle")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def any_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.any
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "any")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def append_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.append
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "append")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def apply_along_axis_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.apply_along_axis
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "apply_along_axis")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def apply_over_axes_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.apply_over_axes
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "apply_over_axes")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def arange_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.arange
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "arange")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def arccos_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.arccos
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "arccos")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def arccosh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.arccosh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "arccosh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def arcsin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.arcsin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "arcsin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def arcsinh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.arcsinh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "arcsinh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def arctan_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.arctan
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "arctan")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def arctan2_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.arctan2
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "arctan2")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def arctanh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.arctanh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "arctanh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def argmax_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.argmax
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "argmax")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def argmin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.argmin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "argmin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def argpartition_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.argpartition
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "argpartition")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def argsort_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.argsort
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "argsort")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def argwhere_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.argwhere
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "argwhere")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def around_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.around
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "around")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def array_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.array
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "array")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def array2string_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.array2string
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "array2string")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def array_equal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.array_equal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "array_equal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def array_equiv_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.array_equiv
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "array_equiv")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def array_repr_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.array_repr
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "array_repr")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def array_split_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.array_split
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "array_split")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def array_str_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.array_str
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "array_str")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def asanyarray_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.asanyarray
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "asanyarray")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def asarray_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.asarray
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "asarray")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def asarray_chkfinite_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.asarray_chkfinite
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "asarray_chkfinite")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ascontiguousarray_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ascontiguousarray
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ascontiguousarray")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def asfortranarray_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.asfortranarray
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "asfortranarray")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def asin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.asin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "asin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def asinh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.asinh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "asinh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def asmatrix_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.asmatrix
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "asmatrix")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def astype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.astype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "astype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def atan_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.atan
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "atan")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def atan2_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.atan2
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "atan2")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def atanh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.atanh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "atanh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def atleast_1d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.atleast_1d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "atleast_1d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def atleast_2d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.atleast_2d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "atleast_2d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def atleast_3d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.atleast_3d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "atleast_3d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def average_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.average
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "average")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bartlett_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bartlett
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bartlett")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def base_repr_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.base_repr
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "base_repr")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def binary_repr_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.binary_repr
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "binary_repr")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bincount_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bincount
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bincount")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bitwise_and_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bitwise_and
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bitwise_and")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bitwise_count_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bitwise_count
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bitwise_count")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bitwise_invert_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bitwise_invert
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bitwise_invert")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bitwise_left_shift_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bitwise_left_shift
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bitwise_left_shift")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bitwise_not_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bitwise_not
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bitwise_not")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bitwise_or_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bitwise_or
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bitwise_or")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bitwise_right_shift_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bitwise_right_shift
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bitwise_right_shift")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bitwise_xor_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bitwise_xor
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bitwise_xor")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def blackman_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.blackman
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "blackman")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def block_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.block
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "block")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bmat_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bmat
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bmat")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bool_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bool
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bool")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bool__wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bool_
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bool_")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def broadcast_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.broadcast
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "broadcast")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def broadcast_arrays_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.broadcast_arrays
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "broadcast_arrays")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def broadcast_shapes_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.broadcast_shapes
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "broadcast_shapes")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def broadcast_to_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.broadcast_to
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "broadcast_to")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def busday_count_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.busday_count
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "busday_count")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def busday_offset_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.busday_offset
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "busday_offset")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def busdaycalendar_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.busdaycalendar
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "busdaycalendar")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def byte_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.byte
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "byte")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def bytes__wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.bytes_
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "bytes_")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def can_cast_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.can_cast
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "can_cast")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cbrt_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cbrt
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cbrt")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cdouble_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cdouble
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cdouble")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ceil_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ceil
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ceil")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def character_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.character
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "character")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def choose_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.choose
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "choose")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def clip_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.clip
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "clip")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def clongdouble_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.clongdouble
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "clongdouble")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def column_stack_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.column_stack
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "column_stack")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def common_type_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.common_type
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "common_type")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def complex128_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.complex128
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "complex128")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def complex64_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.complex64
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "complex64")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def complexfloating_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.complexfloating
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "complexfloating")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def compress_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.compress
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "compress")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def concat_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.concat
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "concat")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def concatenate_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.concatenate
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "concatenate")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def conj_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.conj
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "conj")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def conjugate_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.conjugate
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "conjugate")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def convolve_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.convolve
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "convolve")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def copy_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.copy
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "copy")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def copysign_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.copysign
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "copysign")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def copyto_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.copyto
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "copyto")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def corrcoef_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.corrcoef
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "corrcoef")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def correlate_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.correlate
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "correlate")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cos_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cos
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cos")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cosh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cosh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cosh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def count_nonzero_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.count_nonzero
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "count_nonzero")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cov_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cov
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cov")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cross_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cross
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cross")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def csingle_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.csingle
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "csingle")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cumprod_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cumprod
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cumprod")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cumsum_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cumsum
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cumsum")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cumulative_prod_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cumulative_prod
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cumulative_prod")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def cumulative_sum_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.cumulative_sum
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "cumulative_sum")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def datetime64_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.datetime64
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "datetime64")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def datetime_as_string_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.datetime_as_string
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "datetime_as_string")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def datetime_data_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.datetime_data
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "datetime_data")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def deg2rad_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.deg2rad
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "deg2rad")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def degrees_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.degrees
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "degrees")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def delete_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.delete
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "delete")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def diag_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.diag
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "diag")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def diag_indices_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.diag_indices
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "diag_indices")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def diag_indices_from_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.diag_indices_from
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "diag_indices_from")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def diagflat_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.diagflat
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "diagflat")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def diagonal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.diagonal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "diagonal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def diff_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.diff
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "diff")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def digitize_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.digitize
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "digitize")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def divide_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.divide
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "divide")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def divmod_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.divmod
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "divmod")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def dot_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.dot
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "dot")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def double_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.double
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "double")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def dsplit_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.dsplit
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "dsplit")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def dstack_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.dstack
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "dstack")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def dtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.dtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "dtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ediff1d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ediff1d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ediff1d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def einsum_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.einsum
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "einsum")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def einsum_path_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.einsum_path
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "einsum_path")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def empty_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.empty
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "empty")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def empty_like_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.empty_like
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "empty_like")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def equal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.equal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "equal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def errstate_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.errstate
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "errstate")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def exp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.exp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "exp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def exp2_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.exp2
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "exp2")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def expand_dims_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.expand_dims
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "expand_dims")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def expm1_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.expm1
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "expm1")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def extract_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.extract
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "extract")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def eye_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.eye
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "eye")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fabs_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fabs
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fabs")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fill_diagonal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fill_diagonal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fill_diagonal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def finfo_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.finfo
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "finfo")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fix_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fix
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fix")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def flatiter_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.flatiter
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "flatiter")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def flatnonzero_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.flatnonzero
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "flatnonzero")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def flexible_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.flexible
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "flexible")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def flip_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.flip
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "flip")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fliplr_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fliplr
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fliplr")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def flipud_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.flipud
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "flipud")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def float16_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.float16
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "float16")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def float32_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.float32
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "float32")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def float64_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.float64
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "float64")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def float_power_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.float_power
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "float_power")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def floating_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.floating
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "floating")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def floor_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.floor
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "floor")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def floor_divide_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.floor_divide
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "floor_divide")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fmax_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fmax
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fmax")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fmin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fmin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fmin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fmod_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fmod
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fmod")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def format_float_positional_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.format_float_positional
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "format_float_positional")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def format_float_scientific_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.format_float_scientific
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "format_float_scientific")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def frexp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.frexp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "frexp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def from_dlpack_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.from_dlpack
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "from_dlpack")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def frombuffer_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.frombuffer
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "frombuffer")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fromfile_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fromfile
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fromfile")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fromfunction_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fromfunction
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fromfunction")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fromiter_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fromiter
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fromiter")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def frompyfunc_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.frompyfunc
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "frompyfunc")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fromregex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fromregex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fromregex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def fromstring_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.fromstring
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "fromstring")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def full_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.full
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "full")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def full_like_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.full_like
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "full_like")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def gcd_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.gcd
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "gcd")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def generic_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.generic
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "generic")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def genfromtxt_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.genfromtxt
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "genfromtxt")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def geomspace_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.geomspace
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "geomspace")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def get_include_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.get_include
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "get_include")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def get_printoptions_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.get_printoptions
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "get_printoptions")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def getbufsize_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.getbufsize
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "getbufsize")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def geterr_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.geterr
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "geterr")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def geterrcall_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.geterrcall
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "geterrcall")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def gradient_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.gradient
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "gradient")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def greater_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.greater
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "greater")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def greater_equal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.greater_equal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "greater_equal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def half_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.half
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "half")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def hamming_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.hamming
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "hamming")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def hanning_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.hanning
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "hanning")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def heaviside_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.heaviside
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "heaviside")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def histogram_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.histogram
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "histogram")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def histogram2d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.histogram2d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "histogram2d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def histogram_bin_edges_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.histogram_bin_edges
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "histogram_bin_edges")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def histogramdd_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.histogramdd
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "histogramdd")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def hsplit_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.hsplit
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "hsplit")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def hstack_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.hstack
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "hstack")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def hypot_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.hypot
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "hypot")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def i0_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.i0
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "i0")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def identity_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.identity
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "identity")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def iinfo_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.iinfo
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "iinfo")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def imag_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.imag
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "imag")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def indices_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.indices
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "indices")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def inexact_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.inexact
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "inexact")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def info_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.info
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "info")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def inner_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.inner
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "inner")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def insert_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.insert
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "insert")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def int16_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.int16
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "int16")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def int32_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.int32
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "int32")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def int64_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.int64
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "int64")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def int8_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.int8
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "int8")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def int__wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.int_
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "int_")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def intc_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.intc
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "intc")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def integer_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.integer
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "integer")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def interp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.interp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "interp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def intersect1d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.intersect1d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "intersect1d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def intp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.intp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "intp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def invert_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.invert
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "invert")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def is_busday_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.is_busday
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "is_busday")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isclose_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isclose
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isclose")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def iscomplex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.iscomplex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "iscomplex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def iscomplexobj_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.iscomplexobj
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "iscomplexobj")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isdtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isdtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isdtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isfinite_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isfinite
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isfinite")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isfortran_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isfortran
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isfortran")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isinf_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isinf
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isinf")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isnan_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isnan
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isnan")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isnat_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isnat
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isnat")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isneginf_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isneginf
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isneginf")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isposinf_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isposinf
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isposinf")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isreal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isreal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isreal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isrealobj_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isrealobj
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isrealobj")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def isscalar_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.isscalar
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "isscalar")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def issubdtype_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.issubdtype
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "issubdtype")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def iterable_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.iterable
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "iterable")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ix__wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ix_
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ix_")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def kaiser_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.kaiser
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "kaiser")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def kron_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.kron
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "kron")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def lcm_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.lcm
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "lcm")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ldexp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ldexp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ldexp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def left_shift_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.left_shift
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "left_shift")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def less_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.less
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "less")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def less_equal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.less_equal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "less_equal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def lexsort_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.lexsort
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "lexsort")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def linspace_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.linspace
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "linspace")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def load_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.load
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "load")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def loadtxt_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.loadtxt
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "loadtxt")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def log_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.log
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "log")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def log10_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.log10
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "log10")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def log1p_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.log1p
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "log1p")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def log2_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.log2
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "log2")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def logaddexp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.logaddexp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "logaddexp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def logaddexp2_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.logaddexp2
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "logaddexp2")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def logical_and_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.logical_and
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "logical_and")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def logical_not_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.logical_not
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "logical_not")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def logical_or_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.logical_or
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "logical_or")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def logical_xor_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.logical_xor
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "logical_xor")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def logspace_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.logspace
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "logspace")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def long_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.long
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "long")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def longdouble_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.longdouble
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "longdouble")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def longlong_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.longlong
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "longlong")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def mask_indices_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.mask_indices
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "mask_indices")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def matmul_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.matmul
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "matmul")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def matrix_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.matrix
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "matrix")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def matrix_transpose_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.matrix_transpose
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "matrix_transpose")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def matvec_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.matvec
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "matvec")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def max_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.max
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "max")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def maximum_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.maximum
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "maximum")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def may_share_memory_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.may_share_memory
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "may_share_memory")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def mean_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.mean
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "mean")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def median_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.median
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "median")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def memmap_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.memmap
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "memmap")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def meshgrid_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.meshgrid
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "meshgrid")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def min_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.min
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "min")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def min_scalar_type_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.min_scalar_type
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "min_scalar_type")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def minimum_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.minimum
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "minimum")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def mintypecode_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.mintypecode
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "mintypecode")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def mod_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.mod
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "mod")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def modf_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.modf
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "modf")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def moveaxis_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.moveaxis
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "moveaxis")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def multiply_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.multiply
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "multiply")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nan_to_num_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nan_to_num
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nan_to_num")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanargmax_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanargmax
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanargmax")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanargmin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanargmin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanargmin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nancumprod_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nancumprod
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nancumprod")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nancumsum_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nancumsum
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nancumsum")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanmax_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanmax
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanmax")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanmean_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanmean
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanmean")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanmedian_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanmedian
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanmedian")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanmin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanmin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanmin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanpercentile_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanpercentile
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanpercentile")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanprod_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanprod
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanprod")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanquantile_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanquantile
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanquantile")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanstd_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanstd
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanstd")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nansum_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nansum
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nansum")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nanvar_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nanvar
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nanvar")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ndarray_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ndarray
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ndarray")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ndenumerate_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ndenumerate
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ndenumerate")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ndim_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ndim
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ndim")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ndindex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ndindex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ndindex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nditer_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nditer
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nditer")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def negative_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.negative
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "negative")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nested_iters_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nested_iters
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nested_iters")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nextafter_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nextafter
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nextafter")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def nonzero_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.nonzero
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "nonzero")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def not_equal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.not_equal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "not_equal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def number_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.number
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "number")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def object__wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.object_
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "object_")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ones_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ones
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ones")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ones_like_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ones_like
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ones_like")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def outer_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.outer
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "outer")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def packbits_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.packbits
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "packbits")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def pad_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.pad
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "pad")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def partition_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.partition
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "partition")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def percentile_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.percentile
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "percentile")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def permute_dims_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.permute_dims
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "permute_dims")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def piecewise_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.piecewise
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "piecewise")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def place_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.place
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "place")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def poly_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.poly
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "poly")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def poly1d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.poly1d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "poly1d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def polyadd_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.polyadd
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "polyadd")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def polyder_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.polyder
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "polyder")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def polydiv_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.polydiv
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "polydiv")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def polyfit_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.polyfit
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "polyfit")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def polyint_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.polyint
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "polyint")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def polymul_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.polymul
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "polymul")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def polysub_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.polysub
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "polysub")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def polyval_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.polyval
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "polyval")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def positive_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.positive
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "positive")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def pow_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.pow
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "pow")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def power_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.power
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "power")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def printoptions_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.printoptions
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "printoptions")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def prod_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.prod
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "prod")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def promote_types_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.promote_types
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "promote_types")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ptp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ptp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ptp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def put_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.put
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "put")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def put_along_axis_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.put_along_axis
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "put_along_axis")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def putmask_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.putmask
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "putmask")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def quantile_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.quantile
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "quantile")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def rad2deg_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.rad2deg
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "rad2deg")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def radians_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.radians
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "radians")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ravel_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ravel
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ravel")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ravel_multi_index_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ravel_multi_index
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ravel_multi_index")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def real_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.real
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "real")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def real_if_close_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.real_if_close
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "real_if_close")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def recarray_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.recarray
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "recarray")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def reciprocal_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.reciprocal
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "reciprocal")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def record_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.record
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "record")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def remainder_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.remainder
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "remainder")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def repeat_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.repeat
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "repeat")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def require_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.require
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "require")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def reshape_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.reshape
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "reshape")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def resize_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.resize
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "resize")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def result_type_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.result_type
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "result_type")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def right_shift_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.right_shift
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "right_shift")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def rint_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.rint
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "rint")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def roll_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.roll
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "roll")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def rollaxis_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.rollaxis
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "rollaxis")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def roots_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.roots
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "roots")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def rot90_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.rot90
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "rot90")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def round_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.round
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "round")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def row_stack_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.row_stack
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "row_stack")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def save_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.save
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "save")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def savetxt_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.savetxt
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "savetxt")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def savez_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.savez
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "savez")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def savez_compressed_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.savez_compressed
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "savez_compressed")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def searchsorted_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.searchsorted
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "searchsorted")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def select_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.select
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "select")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def set_printoptions_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.set_printoptions
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "set_printoptions")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def setbufsize_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.setbufsize
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "setbufsize")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def setdiff1d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.setdiff1d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "setdiff1d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def seterr_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.seterr
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "seterr")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def seterrcall_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.seterrcall
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "seterrcall")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def setxor1d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.setxor1d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "setxor1d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def shape_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.shape
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "shape")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def shares_memory_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.shares_memory
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "shares_memory")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def short_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.short
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "short")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def show_config_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.show_config
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "show_config")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def show_runtime_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.show_runtime
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "show_runtime")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def sign_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.sign
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "sign")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def signbit_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.signbit
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "signbit")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def signedinteger_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.signedinteger
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "signedinteger")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def sin_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.sin
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "sin")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def sinc_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.sinc
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "sinc")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def single_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.single
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "single")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def sinh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.sinh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "sinh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def size_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.size
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "size")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def sort_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.sort
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "sort")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def sort_complex_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.sort_complex
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "sort_complex")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def spacing_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.spacing
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "spacing")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def split_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.split
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "split")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def sqrt_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.sqrt
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "sqrt")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def square_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.square
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "square")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def squeeze_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.squeeze
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "squeeze")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def stack_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.stack
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "stack")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def std_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.std
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "std")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def str__wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.str_
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "str_")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def subtract_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.subtract
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "subtract")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def sum_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.sum
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "sum")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def swapaxes_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.swapaxes
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "swapaxes")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def take_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.take
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "take")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def take_along_axis_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.take_along_axis
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "take_along_axis")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def tan_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.tan
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "tan")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def tanh_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.tanh
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "tanh")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def tensordot_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.tensordot
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "tensordot")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def test_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.test
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "test")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def tile_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.tile
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "tile")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def timedelta64_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.timedelta64
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "timedelta64")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def trace_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.trace
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "trace")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def transpose_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.transpose
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "transpose")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def trapezoid_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.trapezoid
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "trapezoid")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def tri_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.tri
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "tri")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def tril_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.tril
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "tril")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def tril_indices_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.tril_indices
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "tril_indices")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def tril_indices_from_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.tril_indices_from
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "tril_indices_from")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def trim_zeros_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.trim_zeros
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "trim_zeros")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def triu_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.triu
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "triu")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def triu_indices_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.triu_indices
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "triu_indices")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def triu_indices_from_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.triu_indices_from
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "triu_indices_from")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def true_divide_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.true_divide
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "true_divide")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def trunc_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.trunc
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "trunc")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def typename_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.typename
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "typename")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ubyte_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ubyte
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ubyte")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ufunc_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ufunc
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ufunc")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def uint_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.uint
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "uint")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def uint16_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.uint16
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "uint16")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def uint32_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.uint32
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "uint32")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def uint64_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.uint64
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "uint64")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def uint8_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.uint8
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "uint8")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def uintc_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.uintc
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "uintc")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def uintp_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.uintp
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "uintp")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ulong_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ulong
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ulong")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ulonglong_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ulonglong
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ulonglong")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def union1d_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.union1d
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "union1d")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unique_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unique
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unique")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unique_all_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unique_all
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unique_all")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unique_counts_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unique_counts
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unique_counts")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unique_inverse_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unique_inverse
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unique_inverse")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unique_values_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unique_values
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unique_values")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unpackbits_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unpackbits
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unpackbits")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unravel_index_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unravel_index
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unravel_index")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unsignedinteger_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unsignedinteger
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unsignedinteger")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unstack_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unstack
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unstack")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def unwrap_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.unwrap
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "unwrap")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ushort_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.ushort
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "ushort")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def vander_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.vander
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "vander")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def var_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.var
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "var")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def vdot_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.vdot
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "vdot")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def vecdot_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.vecdot
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "vecdot")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def vecmat_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.vecmat
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "vecmat")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def vectorize_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.vectorize
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "vectorize")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def void_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.void
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "void")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def vsplit_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.vsplit
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "vsplit")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def vstack_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.vstack
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "vstack")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def where_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.where
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "where")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def zeros_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.zeros
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "zeros")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def zeros_like_wrapper(args: list, kwargs: dict):
    """
    Wrapper for numpy.zeros_like
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import numpy
        member = getattr(numpy, "zeros_like")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def invoke(payload: Dict[str, Any]) -> Dict[str, Any]:
    """
    Main entry point for skill invocation.
    
    Expected payload:
        - action: str (optional): Name of wrapper to call. If omitted, returns list of available wrappers
        - args: list (optional): Positional arguments for the wrapper
        - kwargs: dict (optional): Keyword arguments for the wrapper
    
    Returns:
        dict with 'result' and 'metadata' keys
    """
    start_time = time.time()
    timestamp = datetime.datetime.now().isoformat()
    
    action = payload.get("action")
    args = payload.get("args", [])
    kwargs = payload.get("kwargs", {})
    
    if action is None:
        return {
            "result": {
                "available_wrappers": list(WRAPPED_NAMES),
                "message": "Available wrappers. Use action parameter to call a specific wrapper."
            },
            "metadata": {
                "timestamp": timestamp,
                "library": "numpy",
                "function_count": len(WRAPPED_NAMES)
            }
        }
    
    if action not in WRAPPED_NAMES:
        return {
            "result": {"error": "Unknown action: " + action},
            "metadata": {
                "timestamp": timestamp,
                "error": "Action not found in wrapped names"
            }
        }
    
    wrappers = {
        "abs": abs_wrapper,
        "absolute": absolute_wrapper,
        "acos": acos_wrapper,
        "acosh": acosh_wrapper,
        "add": add_wrapper,
        "all": all_wrapper,
        "allclose": allclose_wrapper,
        "amax": amax_wrapper,
        "amin": amin_wrapper,
        "angle": angle_wrapper,
        "any": any_wrapper,
        "append": append_wrapper,
        "apply_along_axis": apply_along_axis_wrapper,
        "apply_over_axes": apply_over_axes_wrapper,
        "arange": arange_wrapper,
        "arccos": arccos_wrapper,
        "arccosh": arccosh_wrapper,
        "arcsin": arcsin_wrapper,
        "arcsinh": arcsinh_wrapper,
        "arctan": arctan_wrapper,
        "arctan2": arctan2_wrapper,
        "arctanh": arctanh_wrapper,
        "argmax": argmax_wrapper,
        "argmin": argmin_wrapper,
        "argpartition": argpartition_wrapper,
        "argsort": argsort_wrapper,
        "argwhere": argwhere_wrapper,
        "around": around_wrapper,
        "array": array_wrapper,
        "array2string": array2string_wrapper,
        "array_equal": array_equal_wrapper,
        "array_equiv": array_equiv_wrapper,
        "array_repr": array_repr_wrapper,
        "array_split": array_split_wrapper,
        "array_str": array_str_wrapper,
        "asanyarray": asanyarray_wrapper,
        "asarray": asarray_wrapper,
        "asarray_chkfinite": asarray_chkfinite_wrapper,
        "ascontiguousarray": ascontiguousarray_wrapper,
        "asfortranarray": asfortranarray_wrapper,
        "asin": asin_wrapper,
        "asinh": asinh_wrapper,
        "asmatrix": asmatrix_wrapper,
        "astype": astype_wrapper,
        "atan": atan_wrapper,
        "atan2": atan2_wrapper,
        "atanh": atanh_wrapper,
        "atleast_1d": atleast_1d_wrapper,
        "atleast_2d": atleast_2d_wrapper,
        "atleast_3d": atleast_3d_wrapper,
        "average": average_wrapper,
        "bartlett": bartlett_wrapper,
        "base_repr": base_repr_wrapper,
        "binary_repr": binary_repr_wrapper,
        "bincount": bincount_wrapper,
        "bitwise_and": bitwise_and_wrapper,
        "bitwise_count": bitwise_count_wrapper,
        "bitwise_invert": bitwise_invert_wrapper,
        "bitwise_left_shift": bitwise_left_shift_wrapper,
        "bitwise_not": bitwise_not_wrapper,
        "bitwise_or": bitwise_or_wrapper,
        "bitwise_right_shift": bitwise_right_shift_wrapper,
        "bitwise_xor": bitwise_xor_wrapper,
        "blackman": blackman_wrapper,
        "block": block_wrapper,
        "bmat": bmat_wrapper,
        "bool": bool_wrapper,
        "bool_": bool__wrapper,
        "broadcast": broadcast_wrapper,
        "broadcast_arrays": broadcast_arrays_wrapper,
        "broadcast_shapes": broadcast_shapes_wrapper,
        "broadcast_to": broadcast_to_wrapper,
        "busday_count": busday_count_wrapper,
        "busday_offset": busday_offset_wrapper,
        "busdaycalendar": busdaycalendar_wrapper,
        "byte": byte_wrapper,
        "bytes_": bytes__wrapper,
        "can_cast": can_cast_wrapper,
        "cbrt": cbrt_wrapper,
        "cdouble": cdouble_wrapper,
        "ceil": ceil_wrapper,
        "character": character_wrapper,
        "choose": choose_wrapper,
        "clip": clip_wrapper,
        "clongdouble": clongdouble_wrapper,
        "column_stack": column_stack_wrapper,
        "common_type": common_type_wrapper,
        "complex128": complex128_wrapper,
        "complex64": complex64_wrapper,
        "complexfloating": complexfloating_wrapper,
        "compress": compress_wrapper,
        "concat": concat_wrapper,
        "concatenate": concatenate_wrapper,
        "conj": conj_wrapper,
        "conjugate": conjugate_wrapper,
        "convolve": convolve_wrapper,
        "copy": copy_wrapper,
        "copysign": copysign_wrapper,
        "copyto": copyto_wrapper,
        "corrcoef": corrcoef_wrapper,
        "correlate": correlate_wrapper,
        "cos": cos_wrapper,
        "cosh": cosh_wrapper,
        "count_nonzero": count_nonzero_wrapper,
        "cov": cov_wrapper,
        "cross": cross_wrapper,
        "csingle": csingle_wrapper,
        "cumprod": cumprod_wrapper,
        "cumsum": cumsum_wrapper,
        "cumulative_prod": cumulative_prod_wrapper,
        "cumulative_sum": cumulative_sum_wrapper,
        "datetime64": datetime64_wrapper,
        "datetime_as_string": datetime_as_string_wrapper,
        "datetime_data": datetime_data_wrapper,
        "deg2rad": deg2rad_wrapper,
        "degrees": degrees_wrapper,
        "delete": delete_wrapper,
        "diag": diag_wrapper,
        "diag_indices": diag_indices_wrapper,
        "diag_indices_from": diag_indices_from_wrapper,
        "diagflat": diagflat_wrapper,
        "diagonal": diagonal_wrapper,
        "diff": diff_wrapper,
        "digitize": digitize_wrapper,
        "divide": divide_wrapper,
        "divmod": divmod_wrapper,
        "dot": dot_wrapper,
        "double": double_wrapper,
        "dsplit": dsplit_wrapper,
        "dstack": dstack_wrapper,
        "dtype": dtype_wrapper,
        "ediff1d": ediff1d_wrapper,
        "einsum": einsum_wrapper,
        "einsum_path": einsum_path_wrapper,
        "empty": empty_wrapper,
        "empty_like": empty_like_wrapper,
        "equal": equal_wrapper,
        "errstate": errstate_wrapper,
        "exp": exp_wrapper,
        "exp2": exp2_wrapper,
        "expand_dims": expand_dims_wrapper,
        "expm1": expm1_wrapper,
        "extract": extract_wrapper,
        "eye": eye_wrapper,
        "fabs": fabs_wrapper,
        "fill_diagonal": fill_diagonal_wrapper,
        "finfo": finfo_wrapper,
        "fix": fix_wrapper,
        "flatiter": flatiter_wrapper,
        "flatnonzero": flatnonzero_wrapper,
        "flexible": flexible_wrapper,
        "flip": flip_wrapper,
        "fliplr": fliplr_wrapper,
        "flipud": flipud_wrapper,
        "float16": float16_wrapper,
        "float32": float32_wrapper,
        "float64": float64_wrapper,
        "float_power": float_power_wrapper,
        "floating": floating_wrapper,
        "floor": floor_wrapper,
        "floor_divide": floor_divide_wrapper,
        "fmax": fmax_wrapper,
        "fmin": fmin_wrapper,
        "fmod": fmod_wrapper,
        "format_float_positional": format_float_positional_wrapper,
        "format_float_scientific": format_float_scientific_wrapper,
        "frexp": frexp_wrapper,
        "from_dlpack": from_dlpack_wrapper,
        "frombuffer": frombuffer_wrapper,
        "fromfile": fromfile_wrapper,
        "fromfunction": fromfunction_wrapper,
        "fromiter": fromiter_wrapper,
        "frompyfunc": frompyfunc_wrapper,
        "fromregex": fromregex_wrapper,
        "fromstring": fromstring_wrapper,
        "full": full_wrapper,
        "full_like": full_like_wrapper,
        "gcd": gcd_wrapper,
        "generic": generic_wrapper,
        "genfromtxt": genfromtxt_wrapper,
        "geomspace": geomspace_wrapper,
        "get_include": get_include_wrapper,
        "get_printoptions": get_printoptions_wrapper,
        "getbufsize": getbufsize_wrapper,
        "geterr": geterr_wrapper,
        "geterrcall": geterrcall_wrapper,
        "gradient": gradient_wrapper,
        "greater": greater_wrapper,
        "greater_equal": greater_equal_wrapper,
        "half": half_wrapper,
        "hamming": hamming_wrapper,
        "hanning": hanning_wrapper,
        "heaviside": heaviside_wrapper,
        "histogram": histogram_wrapper,
        "histogram2d": histogram2d_wrapper,
        "histogram_bin_edges": histogram_bin_edges_wrapper,
        "histogramdd": histogramdd_wrapper,
        "hsplit": hsplit_wrapper,
        "hstack": hstack_wrapper,
        "hypot": hypot_wrapper,
        "i0": i0_wrapper,
        "identity": identity_wrapper,
        "iinfo": iinfo_wrapper,
        "imag": imag_wrapper,
        "indices": indices_wrapper,
        "inexact": inexact_wrapper,
        "info": info_wrapper,
        "inner": inner_wrapper,
        "insert": insert_wrapper,
        "int16": int16_wrapper,
        "int32": int32_wrapper,
        "int64": int64_wrapper,
        "int8": int8_wrapper,
        "int_": int__wrapper,
        "intc": intc_wrapper,
        "integer": integer_wrapper,
        "interp": interp_wrapper,
        "intersect1d": intersect1d_wrapper,
        "intp": intp_wrapper,
        "invert": invert_wrapper,
        "is_busday": is_busday_wrapper,
        "isclose": isclose_wrapper,
        "iscomplex": iscomplex_wrapper,
        "iscomplexobj": iscomplexobj_wrapper,
        "isdtype": isdtype_wrapper,
        "isfinite": isfinite_wrapper,
        "isfortran": isfortran_wrapper,
        "isin": isin_wrapper,
        "isinf": isinf_wrapper,
        "isnan": isnan_wrapper,
        "isnat": isnat_wrapper,
        "isneginf": isneginf_wrapper,
        "isposinf": isposinf_wrapper,
        "isreal": isreal_wrapper,
        "isrealobj": isrealobj_wrapper,
        "isscalar": isscalar_wrapper,
        "issubdtype": issubdtype_wrapper,
        "iterable": iterable_wrapper,
        "ix_": ix__wrapper,
        "kaiser": kaiser_wrapper,
        "kron": kron_wrapper,
        "lcm": lcm_wrapper,
        "ldexp": ldexp_wrapper,
        "left_shift": left_shift_wrapper,
        "less": less_wrapper,
        "less_equal": less_equal_wrapper,
        "lexsort": lexsort_wrapper,
        "linspace": linspace_wrapper,
        "load": load_wrapper,
        "loadtxt": loadtxt_wrapper,
        "log": log_wrapper,
        "log10": log10_wrapper,
        "log1p": log1p_wrapper,
        "log2": log2_wrapper,
        "logaddexp": logaddexp_wrapper,
        "logaddexp2": logaddexp2_wrapper,
        "logical_and": logical_and_wrapper,
        "logical_not": logical_not_wrapper,
        "logical_or": logical_or_wrapper,
        "logical_xor": logical_xor_wrapper,
        "logspace": logspace_wrapper,
        "long": long_wrapper,
        "longdouble": longdouble_wrapper,
        "longlong": longlong_wrapper,
        "mask_indices": mask_indices_wrapper,
        "matmul": matmul_wrapper,
        "matrix": matrix_wrapper,
        "matrix_transpose": matrix_transpose_wrapper,
        "matvec": matvec_wrapper,
        "max": max_wrapper,
        "maximum": maximum_wrapper,
        "may_share_memory": may_share_memory_wrapper,
        "mean": mean_wrapper,
        "median": median_wrapper,
        "memmap": memmap_wrapper,
        "meshgrid": meshgrid_wrapper,
        "min": min_wrapper,
        "min_scalar_type": min_scalar_type_wrapper,
        "minimum": minimum_wrapper,
        "mintypecode": mintypecode_wrapper,
        "mod": mod_wrapper,
        "modf": modf_wrapper,
        "moveaxis": moveaxis_wrapper,
        "multiply": multiply_wrapper,
        "nan_to_num": nan_to_num_wrapper,
        "nanargmax": nanargmax_wrapper,
        "nanargmin": nanargmin_wrapper,
        "nancumprod": nancumprod_wrapper,
        "nancumsum": nancumsum_wrapper,
        "nanmax": nanmax_wrapper,
        "nanmean": nanmean_wrapper,
        "nanmedian": nanmedian_wrapper,
        "nanmin": nanmin_wrapper,
        "nanpercentile": nanpercentile_wrapper,
        "nanprod": nanprod_wrapper,
        "nanquantile": nanquantile_wrapper,
        "nanstd": nanstd_wrapper,
        "nansum": nansum_wrapper,
        "nanvar": nanvar_wrapper,
        "ndarray": ndarray_wrapper,
        "ndenumerate": ndenumerate_wrapper,
        "ndim": ndim_wrapper,
        "ndindex": ndindex_wrapper,
        "nditer": nditer_wrapper,
        "negative": negative_wrapper,
        "nested_iters": nested_iters_wrapper,
        "nextafter": nextafter_wrapper,
        "nonzero": nonzero_wrapper,
        "not_equal": not_equal_wrapper,
        "number": number_wrapper,
        "object_": object__wrapper,
        "ones": ones_wrapper,
        "ones_like": ones_like_wrapper,
        "outer": outer_wrapper,
        "packbits": packbits_wrapper,
        "pad": pad_wrapper,
        "partition": partition_wrapper,
        "percentile": percentile_wrapper,
        "permute_dims": permute_dims_wrapper,
        "piecewise": piecewise_wrapper,
        "place": place_wrapper,
        "poly": poly_wrapper,
        "poly1d": poly1d_wrapper,
        "polyadd": polyadd_wrapper,
        "polyder": polyder_wrapper,
        "polydiv": polydiv_wrapper,
        "polyfit": polyfit_wrapper,
        "polyint": polyint_wrapper,
        "polymul": polymul_wrapper,
        "polysub": polysub_wrapper,
        "polyval": polyval_wrapper,
        "positive": positive_wrapper,
        "pow": pow_wrapper,
        "power": power_wrapper,
        "printoptions": printoptions_wrapper,
        "prod": prod_wrapper,
        "promote_types": promote_types_wrapper,
        "ptp": ptp_wrapper,
        "put": put_wrapper,
        "put_along_axis": put_along_axis_wrapper,
        "putmask": putmask_wrapper,
        "quantile": quantile_wrapper,
        "rad2deg": rad2deg_wrapper,
        "radians": radians_wrapper,
        "ravel": ravel_wrapper,
        "ravel_multi_index": ravel_multi_index_wrapper,
        "real": real_wrapper,
        "real_if_close": real_if_close_wrapper,
        "recarray": recarray_wrapper,
        "reciprocal": reciprocal_wrapper,
        "record": record_wrapper,
        "remainder": remainder_wrapper,
        "repeat": repeat_wrapper,
        "require": require_wrapper,
        "reshape": reshape_wrapper,
        "resize": resize_wrapper,
        "result_type": result_type_wrapper,
        "right_shift": right_shift_wrapper,
        "rint": rint_wrapper,
        "roll": roll_wrapper,
        "rollaxis": rollaxis_wrapper,
        "roots": roots_wrapper,
        "rot90": rot90_wrapper,
        "round": round_wrapper,
        "row_stack": row_stack_wrapper,
        "save": save_wrapper,
        "savetxt": savetxt_wrapper,
        "savez": savez_wrapper,
        "savez_compressed": savez_compressed_wrapper,
        "searchsorted": searchsorted_wrapper,
        "select": select_wrapper,
        "set_printoptions": set_printoptions_wrapper,
        "setbufsize": setbufsize_wrapper,
        "setdiff1d": setdiff1d_wrapper,
        "seterr": seterr_wrapper,
        "seterrcall": seterrcall_wrapper,
        "setxor1d": setxor1d_wrapper,
        "shape": shape_wrapper,
        "shares_memory": shares_memory_wrapper,
        "short": short_wrapper,
        "show_config": show_config_wrapper,
        "show_runtime": show_runtime_wrapper,
        "sign": sign_wrapper,
        "signbit": signbit_wrapper,
        "signedinteger": signedinteger_wrapper,
        "sin": sin_wrapper,
        "sinc": sinc_wrapper,
        "single": single_wrapper,
        "sinh": sinh_wrapper,
        "size": size_wrapper,
        "sort": sort_wrapper,
        "sort_complex": sort_complex_wrapper,
        "spacing": spacing_wrapper,
        "split": split_wrapper,
        "sqrt": sqrt_wrapper,
        "square": square_wrapper,
        "squeeze": squeeze_wrapper,
        "stack": stack_wrapper,
        "std": std_wrapper,
        "str_": str__wrapper,
        "subtract": subtract_wrapper,
        "sum": sum_wrapper,
        "swapaxes": swapaxes_wrapper,
        "take": take_wrapper,
        "take_along_axis": take_along_axis_wrapper,
        "tan": tan_wrapper,
        "tanh": tanh_wrapper,
        "tensordot": tensordot_wrapper,
        "test": test_wrapper,
        "tile": tile_wrapper,
        "timedelta64": timedelta64_wrapper,
        "trace": trace_wrapper,
        "transpose": transpose_wrapper,
        "trapezoid": trapezoid_wrapper,
        "tri": tri_wrapper,
        "tril": tril_wrapper,
        "tril_indices": tril_indices_wrapper,
        "tril_indices_from": tril_indices_from_wrapper,
        "trim_zeros": trim_zeros_wrapper,
        "triu": triu_wrapper,
        "triu_indices": triu_indices_wrapper,
        "triu_indices_from": triu_indices_from_wrapper,
        "true_divide": true_divide_wrapper,
        "trunc": trunc_wrapper,
        "typename": typename_wrapper,
        "ubyte": ubyte_wrapper,
        "ufunc": ufunc_wrapper,
        "uint": uint_wrapper,
        "uint16": uint16_wrapper,
        "uint32": uint32_wrapper,
        "uint64": uint64_wrapper,
        "uint8": uint8_wrapper,
        "uintc": uintc_wrapper,
        "uintp": uintp_wrapper,
        "ulong": ulong_wrapper,
        "ulonglong": ulonglong_wrapper,
        "union1d": union1d_wrapper,
        "unique": unique_wrapper,
        "unique_all": unique_all_wrapper,
        "unique_counts": unique_counts_wrapper,
        "unique_inverse": unique_inverse_wrapper,
        "unique_values": unique_values_wrapper,
        "unpackbits": unpackbits_wrapper,
        "unravel_index": unravel_index_wrapper,
        "unsignedinteger": unsignedinteger_wrapper,
        "unstack": unstack_wrapper,
        "unwrap": unwrap_wrapper,
        "ushort": ushort_wrapper,
        "vander": vander_wrapper,
        "var": var_wrapper,
        "vdot": vdot_wrapper,
        "vecdot": vecdot_wrapper,
        "vecmat": vecmat_wrapper,
        "vectorize": vectorize_wrapper,
        "void": void_wrapper,
        "vsplit": vsplit_wrapper,
        "vstack": vstack_wrapper,
        "where": where_wrapper,
        "zeros": zeros_wrapper,
        "zeros_like": zeros_like_wrapper,
    }
    
    try:
        result = await wrappers[action](args, kwargs)
        elapsed = time.time() - start_time
        
        return {
            "result": result,
            "metadata": {
                "timestamp": timestamp,
                "library": "numpy",
                "library_version": "2.4.2" if "2.4.2" != "None" else None,
                "action": action,
                "elapsed_seconds": elapsed
            }
        }
    except Exception as e:
        logger.error("Error invoking {}: {}".format(action, str(e)))
        return {
            "result": {"error": str(e)},
            "metadata": {
                "timestamp": timestamp,
                "library": "numpy",
                "error": str(e)
            }
        }


def register_skill():
    """Return skill metadata for registration"""
    return {
        "name": "numpy-wrapper",
        "description": "Auto-generated skill wrapping numpy public API",
        "domain": "data_engineering",
        "version": "1.0.0",
    }
