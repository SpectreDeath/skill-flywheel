"""
Auto-generated skill for aiohttp

This skill wraps the public API of the aiohttp library.
Generated on: 2026-03-20T19:13:02.393016
Library version: 3.13.3

Wrapped components: 103

Usage:
    Invoke without action to list available wrappers:
        await invoke({"action": None})
    
    Invoke a specific wrapper:
        await invoke({"action": "function_name", "args": [arg1, arg2], "kwargs": {"key": "value"}})
"""
import inspect
import logging
from typing import Any, Dict, List

import time
import datetime

logging.basicConfig(level=logging.INFO)
logger = logging.getLogger(__name__)

WRAPPED_NAMES = ["AddrInfoType", "AsyncIterablePayload", "AsyncResolver", "BadContentDispositionHeader", "BadContentDispositionParam", "BaseConnector", "BasicAuth", "BodyPartReader", "BufferedReaderPayload", "BytesIOPayload", "BytesPayload", "ChainMapProxy", "ClientConnectionError", "ClientConnectionResetError", "ClientConnectorCertificateError", "ClientConnectorDNSError", "ClientConnectorError", "ClientConnectorSSLError", "ClientError", "ClientHandlerType", "ClientHttpProxyError", "ClientMiddlewareType", "ClientOSError", "ClientPayloadError", "ClientProxyConnectionError", "ClientRequest", "ClientResponse", "ClientResponseError", "ClientSSLError", "ClientSession", "ClientTimeout", "ClientWSTimeout", "ClientWebSocketResponse", "ConnectionTimeoutError", "ContentTypeError", "CookieJar", "DataQueue", "DefaultResolver", "DigestAuthMiddleware", "DummyCookieJar", "ETag", "EofStream", "Fingerprint", "FlowControlDataQueue", "FormData", "HttpVersion", "IOBasePayload", "InvalidURL", "InvalidUrlClientError", "InvalidUrlRedirectClientError", "JsonPayload", "MultipartReader", "MultipartWriter", "NamedPipeConnector", "NonHttpUrlClientError", "NonHttpUrlRedirectClientError", "Payload", "RedirectClientError", "RequestInfo", "ServerConnectionError", "ServerDisconnectedError", "ServerFingerprintMismatch", "ServerTimeoutError", "SocketFactoryType", "SocketTimeoutError", "StreamReader", "StringIOPayload", "StringPayload", "TCPConnector", "TextIOPayload", "ThreadedResolver", "TooManyRedirects", "TraceConfig", "TraceConnectionCreateEndParams", "TraceConnectionCreateStartParams", "TraceConnectionQueuedEndParams", "TraceConnectionQueuedStartParams", "TraceConnectionReuseconnParams", "TraceDnsCacheHitParams", "TraceDnsCacheMissParams", "TraceDnsResolveHostEndParams", "TraceDnsResolveHostStartParams", "TraceRequestChunkSentParams", "TraceRequestEndParams", "TraceRequestExceptionParams", "TraceRequestHeadersSentParams", "TraceRequestRedirectParams", "TraceRequestStartParams", "TraceResponseChunkReceivedParams", "UnixConnector", "WSCloseCode", "WSMessage", "WSMessageTypeError", "WSMsgType", "WSServerHandshakeError", "WebSocketError", "content_disposition_filename", "get_payload", "parse_content_disposition", "payload_type", "request", "set_zlib_backend", "streamer"]

async def AddrInfoType_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.AddrInfoType
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "AddrInfoType")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def AsyncIterablePayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.AsyncIterablePayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "AsyncIterablePayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def AsyncResolver_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.AsyncResolver
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "AsyncResolver")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BadContentDispositionHeader_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.BadContentDispositionHeader
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "BadContentDispositionHeader")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BadContentDispositionParam_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.BadContentDispositionParam
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "BadContentDispositionParam")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BaseConnector_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.BaseConnector
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "BaseConnector")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BasicAuth_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.BasicAuth
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "BasicAuth")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BodyPartReader_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.BodyPartReader
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "BodyPartReader")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BufferedReaderPayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.BufferedReaderPayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "BufferedReaderPayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BytesIOPayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.BytesIOPayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "BytesIOPayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def BytesPayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.BytesPayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "BytesPayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ChainMapProxy_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ChainMapProxy
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ChainMapProxy")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientConnectionError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientConnectionError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientConnectionError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientConnectionResetError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientConnectionResetError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientConnectionResetError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientConnectorCertificateError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientConnectorCertificateError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientConnectorCertificateError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientConnectorDNSError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientConnectorDNSError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientConnectorDNSError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientConnectorError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientConnectorError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientConnectorError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientConnectorSSLError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientConnectorSSLError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientConnectorSSLError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientHandlerType_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientHandlerType
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientHandlerType")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientHttpProxyError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientHttpProxyError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientHttpProxyError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientMiddlewareType_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientMiddlewareType
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientMiddlewareType")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientOSError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientOSError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientOSError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientPayloadError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientPayloadError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientPayloadError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientProxyConnectionError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientProxyConnectionError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientProxyConnectionError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientRequest_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientRequest
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientRequest")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientResponse_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientResponse
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientResponse")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientResponseError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientResponseError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientResponseError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientSSLError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientSSLError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientSSLError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientSession_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientSession
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientSession")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientTimeout_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientTimeout
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientTimeout")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientWSTimeout_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientWSTimeout
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientWSTimeout")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ClientWebSocketResponse_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ClientWebSocketResponse
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ClientWebSocketResponse")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ConnectionTimeoutError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ConnectionTimeoutError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ConnectionTimeoutError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ContentTypeError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ContentTypeError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ContentTypeError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def CookieJar_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.CookieJar
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "CookieJar")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def DataQueue_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.DataQueue
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "DataQueue")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def DefaultResolver_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.DefaultResolver
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "DefaultResolver")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def DigestAuthMiddleware_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.DigestAuthMiddleware
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "DigestAuthMiddleware")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def DummyCookieJar_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.DummyCookieJar
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "DummyCookieJar")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ETag_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ETag
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ETag")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def EofStream_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.EofStream
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "EofStream")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Fingerprint_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.Fingerprint
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "Fingerprint")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def FlowControlDataQueue_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.FlowControlDataQueue
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "FlowControlDataQueue")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def FormData_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.FormData
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "FormData")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def HttpVersion_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.HttpVersion
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "HttpVersion")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def IOBasePayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.IOBasePayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "IOBasePayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def InvalidURL_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.InvalidURL
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "InvalidURL")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def InvalidUrlClientError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.InvalidUrlClientError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "InvalidUrlClientError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def InvalidUrlRedirectClientError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.InvalidUrlRedirectClientError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "InvalidUrlRedirectClientError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def JsonPayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.JsonPayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "JsonPayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def MultipartReader_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.MultipartReader
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "MultipartReader")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def MultipartWriter_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.MultipartWriter
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "MultipartWriter")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def NamedPipeConnector_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.NamedPipeConnector
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "NamedPipeConnector")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def NonHttpUrlClientError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.NonHttpUrlClientError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "NonHttpUrlClientError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def NonHttpUrlRedirectClientError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.NonHttpUrlRedirectClientError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "NonHttpUrlRedirectClientError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def Payload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.Payload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "Payload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def RedirectClientError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.RedirectClientError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "RedirectClientError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def RequestInfo_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.RequestInfo
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "RequestInfo")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ServerConnectionError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ServerConnectionError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ServerConnectionError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ServerDisconnectedError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ServerDisconnectedError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ServerDisconnectedError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ServerFingerprintMismatch_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ServerFingerprintMismatch
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ServerFingerprintMismatch")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ServerTimeoutError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ServerTimeoutError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ServerTimeoutError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def SocketFactoryType_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.SocketFactoryType
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "SocketFactoryType")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def SocketTimeoutError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.SocketTimeoutError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "SocketTimeoutError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def StreamReader_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.StreamReader
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "StreamReader")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def StringIOPayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.StringIOPayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "StringIOPayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def StringPayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.StringPayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "StringPayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TCPConnector_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TCPConnector
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TCPConnector")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TextIOPayload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TextIOPayload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TextIOPayload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def ThreadedResolver_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.ThreadedResolver
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "ThreadedResolver")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TooManyRedirects_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TooManyRedirects
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TooManyRedirects")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceConfig_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceConfig
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceConfig")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceConnectionCreateEndParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceConnectionCreateEndParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceConnectionCreateEndParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceConnectionCreateStartParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceConnectionCreateStartParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceConnectionCreateStartParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceConnectionQueuedEndParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceConnectionQueuedEndParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceConnectionQueuedEndParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceConnectionQueuedStartParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceConnectionQueuedStartParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceConnectionQueuedStartParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceConnectionReuseconnParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceConnectionReuseconnParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceConnectionReuseconnParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceDnsCacheHitParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceDnsCacheHitParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceDnsCacheHitParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceDnsCacheMissParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceDnsCacheMissParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceDnsCacheMissParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceDnsResolveHostEndParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceDnsResolveHostEndParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceDnsResolveHostEndParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceDnsResolveHostStartParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceDnsResolveHostStartParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceDnsResolveHostStartParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceRequestChunkSentParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceRequestChunkSentParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceRequestChunkSentParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceRequestEndParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceRequestEndParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceRequestEndParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceRequestExceptionParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceRequestExceptionParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceRequestExceptionParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceRequestHeadersSentParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceRequestHeadersSentParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceRequestHeadersSentParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceRequestRedirectParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceRequestRedirectParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceRequestRedirectParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceRequestStartParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceRequestStartParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceRequestStartParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def TraceResponseChunkReceivedParams_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.TraceResponseChunkReceivedParams
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "TraceResponseChunkReceivedParams")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def UnixConnector_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.UnixConnector
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "UnixConnector")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def WSCloseCode_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.WSCloseCode
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "WSCloseCode")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def WSMessage_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.WSMessage
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "WSMessage")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def WSMessageTypeError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.WSMessageTypeError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "WSMessageTypeError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def WSMsgType_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.WSMsgType
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "WSMsgType")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def WSServerHandshakeError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.WSServerHandshakeError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "WSServerHandshakeError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def WebSocketError_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.WebSocketError
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "WebSocketError")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def content_disposition_filename_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.content_disposition_filename
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "content_disposition_filename")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def get_payload_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.get_payload
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "get_payload")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def parse_content_disposition_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.parse_content_disposition
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "parse_content_disposition")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def payload_type_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.payload_type
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "payload_type")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def request_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.request
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "request")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def set_zlib_backend_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.set_zlib_backend
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "set_zlib_backend")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def streamer_wrapper(args: list, kwargs: dict):
    """
    Wrapper for aiohttp.streamer
    
    For classes: instantiates the class with provided args/kwargs and returns the instance.
    For functions: calls the function with provided args/kwargs and returns the result.
    
    Args:
        args: Positional arguments to pass to the function/class
        kwargs: Keyword arguments to pass to the function/class
    
    Returns:
        Result from calling the function or instantiating the class
    """
    try:
        import aiohttp
        member = getattr(aiohttp, "streamer")
        if inspect.isclass(member):
            instance = member(*args, **kwargs)
            return {"instance": str(instance), "type": type(instance).__name__}
        else:
            return member(*args, **kwargs)
    except Exception as e:
        return {"error": str(e)}

async def invoke(payload: Dict[str, Any]) -> Dict[str, Any]:
    """
    Main entry point for skill invocation.
    
    Expected payload:
        - action: str (optional): Name of wrapper to call. If omitted, returns list of available wrappers
        - args: list (optional): Positional arguments for the wrapper
        - kwargs: dict (optional): Keyword arguments for the wrapper
    
    Returns:
        dict with 'result' and 'metadata' keys
    """
    start_time = time.time()
    timestamp = datetime.datetime.now().isoformat()
    
    action = payload.get("action")
    args = payload.get("args", [])
    kwargs = payload.get("kwargs", {})
    
    if action is None:
        return {
            "result": {
                "available_wrappers": list(WRAPPED_NAMES),
                "message": "Available wrappers. Use action parameter to call a specific wrapper."
            },
            "metadata": {
                "timestamp": timestamp,
                "library": "aiohttp",
                "function_count": len(WRAPPED_NAMES)
            }
        }
    
    if action not in WRAPPED_NAMES:
        return {
            "result": {"error": "Unknown action: " + action},
            "metadata": {
                "timestamp": timestamp,
                "error": "Action not found in wrapped names"
            }
        }
    
    wrappers = {
        "AddrInfoType": AddrInfoType_wrapper,
        "AsyncIterablePayload": AsyncIterablePayload_wrapper,
        "AsyncResolver": AsyncResolver_wrapper,
        "BadContentDispositionHeader": BadContentDispositionHeader_wrapper,
        "BadContentDispositionParam": BadContentDispositionParam_wrapper,
        "BaseConnector": BaseConnector_wrapper,
        "BasicAuth": BasicAuth_wrapper,
        "BodyPartReader": BodyPartReader_wrapper,
        "BufferedReaderPayload": BufferedReaderPayload_wrapper,
        "BytesIOPayload": BytesIOPayload_wrapper,
        "BytesPayload": BytesPayload_wrapper,
        "ChainMapProxy": ChainMapProxy_wrapper,
        "ClientConnectionError": ClientConnectionError_wrapper,
        "ClientConnectionResetError": ClientConnectionResetError_wrapper,
        "ClientConnectorCertificateError": ClientConnectorCertificateError_wrapper,
        "ClientConnectorDNSError": ClientConnectorDNSError_wrapper,
        "ClientConnectorError": ClientConnectorError_wrapper,
        "ClientConnectorSSLError": ClientConnectorSSLError_wrapper,
        "ClientError": ClientError_wrapper,
        "ClientHandlerType": ClientHandlerType_wrapper,
        "ClientHttpProxyError": ClientHttpProxyError_wrapper,
        "ClientMiddlewareType": ClientMiddlewareType_wrapper,
        "ClientOSError": ClientOSError_wrapper,
        "ClientPayloadError": ClientPayloadError_wrapper,
        "ClientProxyConnectionError": ClientProxyConnectionError_wrapper,
        "ClientRequest": ClientRequest_wrapper,
        "ClientResponse": ClientResponse_wrapper,
        "ClientResponseError": ClientResponseError_wrapper,
        "ClientSSLError": ClientSSLError_wrapper,
        "ClientSession": ClientSession_wrapper,
        "ClientTimeout": ClientTimeout_wrapper,
        "ClientWSTimeout": ClientWSTimeout_wrapper,
        "ClientWebSocketResponse": ClientWebSocketResponse_wrapper,
        "ConnectionTimeoutError": ConnectionTimeoutError_wrapper,
        "ContentTypeError": ContentTypeError_wrapper,
        "CookieJar": CookieJar_wrapper,
        "DataQueue": DataQueue_wrapper,
        "DefaultResolver": DefaultResolver_wrapper,
        "DigestAuthMiddleware": DigestAuthMiddleware_wrapper,
        "DummyCookieJar": DummyCookieJar_wrapper,
        "ETag": ETag_wrapper,
        "EofStream": EofStream_wrapper,
        "Fingerprint": Fingerprint_wrapper,
        "FlowControlDataQueue": FlowControlDataQueue_wrapper,
        "FormData": FormData_wrapper,
        "HttpVersion": HttpVersion_wrapper,
        "IOBasePayload": IOBasePayload_wrapper,
        "InvalidURL": InvalidURL_wrapper,
        "InvalidUrlClientError": InvalidUrlClientError_wrapper,
        "InvalidUrlRedirectClientError": InvalidUrlRedirectClientError_wrapper,
        "JsonPayload": JsonPayload_wrapper,
        "MultipartReader": MultipartReader_wrapper,
        "MultipartWriter": MultipartWriter_wrapper,
        "NamedPipeConnector": NamedPipeConnector_wrapper,
        "NonHttpUrlClientError": NonHttpUrlClientError_wrapper,
        "NonHttpUrlRedirectClientError": NonHttpUrlRedirectClientError_wrapper,
        "Payload": Payload_wrapper,
        "RedirectClientError": RedirectClientError_wrapper,
        "RequestInfo": RequestInfo_wrapper,
        "ServerConnectionError": ServerConnectionError_wrapper,
        "ServerDisconnectedError": ServerDisconnectedError_wrapper,
        "ServerFingerprintMismatch": ServerFingerprintMismatch_wrapper,
        "ServerTimeoutError": ServerTimeoutError_wrapper,
        "SocketFactoryType": SocketFactoryType_wrapper,
        "SocketTimeoutError": SocketTimeoutError_wrapper,
        "StreamReader": StreamReader_wrapper,
        "StringIOPayload": StringIOPayload_wrapper,
        "StringPayload": StringPayload_wrapper,
        "TCPConnector": TCPConnector_wrapper,
        "TextIOPayload": TextIOPayload_wrapper,
        "ThreadedResolver": ThreadedResolver_wrapper,
        "TooManyRedirects": TooManyRedirects_wrapper,
        "TraceConfig": TraceConfig_wrapper,
        "TraceConnectionCreateEndParams": TraceConnectionCreateEndParams_wrapper,
        "TraceConnectionCreateStartParams": TraceConnectionCreateStartParams_wrapper,
        "TraceConnectionQueuedEndParams": TraceConnectionQueuedEndParams_wrapper,
        "TraceConnectionQueuedStartParams": TraceConnectionQueuedStartParams_wrapper,
        "TraceConnectionReuseconnParams": TraceConnectionReuseconnParams_wrapper,
        "TraceDnsCacheHitParams": TraceDnsCacheHitParams_wrapper,
        "TraceDnsCacheMissParams": TraceDnsCacheMissParams_wrapper,
        "TraceDnsResolveHostEndParams": TraceDnsResolveHostEndParams_wrapper,
        "TraceDnsResolveHostStartParams": TraceDnsResolveHostStartParams_wrapper,
        "TraceRequestChunkSentParams": TraceRequestChunkSentParams_wrapper,
        "TraceRequestEndParams": TraceRequestEndParams_wrapper,
        "TraceRequestExceptionParams": TraceRequestExceptionParams_wrapper,
        "TraceRequestHeadersSentParams": TraceRequestHeadersSentParams_wrapper,
        "TraceRequestRedirectParams": TraceRequestRedirectParams_wrapper,
        "TraceRequestStartParams": TraceRequestStartParams_wrapper,
        "TraceResponseChunkReceivedParams": TraceResponseChunkReceivedParams_wrapper,
        "UnixConnector": UnixConnector_wrapper,
        "WSCloseCode": WSCloseCode_wrapper,
        "WSMessage": WSMessage_wrapper,
        "WSMessageTypeError": WSMessageTypeError_wrapper,
        "WSMsgType": WSMsgType_wrapper,
        "WSServerHandshakeError": WSServerHandshakeError_wrapper,
        "WebSocketError": WebSocketError_wrapper,
        "content_disposition_filename": content_disposition_filename_wrapper,
        "get_payload": get_payload_wrapper,
        "parse_content_disposition": parse_content_disposition_wrapper,
        "payload_type": payload_type_wrapper,
        "request": request_wrapper,
        "set_zlib_backend": set_zlib_backend_wrapper,
        "streamer": streamer_wrapper,
    }
    
    try:
        result = await wrappers[action](args, kwargs)
        elapsed = time.time() - start_time
        
        return {
            "result": result,
            "metadata": {
                "timestamp": timestamp,
                "library": "aiohttp",
                "library_version": "3.13.3" if "3.13.3" != "None" else None,
                "action": action,
                "elapsed_seconds": elapsed
            }
        }
    except Exception as e:
        logger.error("Error invoking {}: {}".format(action, str(e)))
        return {
            "result": {"error": str(e)},
            "metadata": {
                "timestamp": timestamp,
                "library": "aiohttp",
                "error": str(e)
            }
        }


def register_skill():
    """Return skill metadata for registration"""
    return {
        "name": "aiohttp-wrapper",
        "description": "Auto-generated skill wrapping aiohttp public API",
        "domain": "devops",
        "version": "1.0.0",
    }
